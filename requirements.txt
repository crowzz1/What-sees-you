# Core dependencies
opencv-python>=4.8.0
numpy>=2.0.0,<2.3.0
torch>=2.0.0
ultralytics>=8.0.0
scikit-learn>=1.3.0

# Face analysis - InsightFace
insightface>=0.7.3
onnxruntime-gpu>=1.16.0  # For GPU (use onnxruntime for CPU only)

# Emotion + Race recognition - DeepFace (for main_with_all_attributes.py)
deepface>=0.0.79

# Alternative: FER for emotion only (lighter weight)
# fer>=22.4.0

# Depth estimation for visual effects
timm>=0.9.0  # Required for MiDaS models
